# Victor AI — Python dependencies
#
# Install all dependencies:
#   pip install -r requirements.txt
#
# For development (includes test tools):
#   pip install -r requirements.txt -r requirements-dev.txt

# ---------------------------------------------------------------------------
# 1. Ego Core — Persistent Long-Term Memory
# ---------------------------------------------------------------------------
mem0ai>=0.1.98

# ---------------------------------------------------------------------------
# 2. Endocrine System — Dynamic Local Compute
# ---------------------------------------------------------------------------
# Use >=1.61.15 to avoid CVE: LiteLLM Improper Authorization
litellm>=1.61.15

# ---------------------------------------------------------------------------
# 3. Ethica Moral Fabric — Ethical Guardrails
# ---------------------------------------------------------------------------
nemoguardrails>=0.10.1

# ---------------------------------------------------------------------------
# 4. Symbiotic Bridge — Environmental Awareness
# ---------------------------------------------------------------------------
websockets>=14.1

# ---------------------------------------------------------------------------
# 5. Reality Translation Engine — Agentic Execution
# ---------------------------------------------------------------------------
langgraph>=0.2.64
# Use >=0.3.27 to avoid CVE: LangChain Community XXE vulnerability
langchain-community>=0.3.27
langchain-core>=0.3.0

# ---------------------------------------------------------------------------
# Supporting dependencies
# ---------------------------------------------------------------------------
psycopg2-binary>=2.9.10   # PostgreSQL/pgvector driver for Mem0
python-dotenv>=1.0.0      # Load .env files
